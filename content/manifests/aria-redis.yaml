apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: aria-redis
  namespace: kube-system
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: aria-redis
        role: master
        tier: backend
    spec:
      containers:
      - name: aria-redis
        image: redis
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: aria-redis
  namespace: kube-system
  labels:
    app: aria-redis
    role: master
    tier: backend
spec:
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: aria-redis
    role: master
    tier: backend