apiVersion: apps/v1
kind: Deployment
metadata:
  name: contracts-interval
  namespace: billing
  labels:
    app: contracts-interval
    kind: aria-app
    tier: frontend
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: contracts-interval
  template:
    metadata:
      labels:
        app: contracts-interval
    spec:
      containers:
      - name: contracts-interval
        image: contracts-interval.ui.billing
        ports:
        - containerPort: 443
        volumeMounts:
        - { name: certs, mountPath: /certs }
        - { name: nginx-conf, mountPath: /etc/nginx/conf.d }
      volumes:
      - name: certs
        secret: { secretName: acc-certs }
      - name: nginx-conf
        configMap: { name: nginx-conf }
      imagePullSecrets:
      - name: regcred
