apiVersion: apps/v1
kind: Deployment
metadata:
  name: remote-indications
  namespace: billing
  labels:
    app: remote-indications
    kind: aria-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: remote-indications
  template:
    metadata:
      labels:
        app: remote-indications
    spec:
      containers:
      - name: remote-indications
        image: remote-indications.ui.billing
        ports:
        - containerPort: 443
        env:
        - { name: TZ, value: Europe/Chisinau }
        volumeMounts:
        - { name: certs, mountPath: /certs, readOnly: true }
      volumes:
      - name: certs
        hostPath: { path: /home/acc-server-admin/certs, readOnly: true }
      imagePullSecrets:
      - name: regcred
