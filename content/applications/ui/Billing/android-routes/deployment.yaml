apiVersion: apps/v1
kind: Deployment
metadata:
  name: android-routes
  namespace: billing
  labels:
    app: android-routes
    kind: aria-app
    tier: frontend
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: android-routes
  template:
    metadata:
      labels:
        app: android-routes
    spec:
      containers:
      - name: android-routes
        image: android-routes.ui.billing
        ports:
        - containerPort: 443
        volumeMounts:
        - { name: certs, mountPath: /certs }
        - { name: nginx-conf, mountPath: /etc/nginx/conf.d }
      volumes:
      - name: certs
        secret: { secretName: acc-certs }
      - name: nginx-conf
        configMap: { name: nginx-conf }
      imagePullSecrets:
      - name: regcred
