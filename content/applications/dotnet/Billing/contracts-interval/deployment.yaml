apiVersion: apps/v1
kind: Deployment
metadata:
  name: contracts-interval.api
  namespace: billing
  labels:
    app: contracts-interval.api
    kind: aria-app
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: contracts-interval.api
  template:
    metadata:
      labels:
        app: contracts-interval.api
    spec:
      containers:
      - name: contracts-interval-api
        image: contracts-interval.api.billing
        ports:
        - containerPort: 443
        env:
        - { name: TZ, value: Europe/Chisinau }
        volumeMounts:
        - { name: certs, mountPath: /certs, readOnly: true }
      volumes:
      - name: certs
        hostPath: { path: /home/acc-server-admin/certs, readOnly: true }
      imagePullSecrets:
      - name: regcred
