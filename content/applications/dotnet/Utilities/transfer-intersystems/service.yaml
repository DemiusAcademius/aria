apiVersion: v1
kind: Service
metadata: 
  name: transfer-inersystems.api
  namespace: billing
  annotations:
    aria.io/proxy-config: |
      listener: acc
      routes:
      - match: { prefix: "/api/transfer-inersystems/"}
        route: { prefix_rewrite: "/", timeout: long }
spec:
  selector:
    app: transfer-inersystems.api
  ports:
  - protocol: TCP
    port: 443
    targetPort: 443